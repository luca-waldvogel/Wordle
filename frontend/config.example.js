// It is for local development, change the URL to your local backend URL
window.API_BASE_URL = "http://localhost:5000";

// It is real deployment, that you can use for testing, but it may be slow and unreliable, so it's better to deploy your own backend and change the URL to your deployed backend URL
// window.API_BASE_URL = "https://wordle-backend-st1q.onrender.com";

// You can also use environment variables to set the API base URL, but for simplicity, we are using a hardcoded value here.
// window.API_BASE_URL = "your_backend_url_from_env_variable";
